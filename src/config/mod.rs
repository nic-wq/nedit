mod config;
mod keybinds;

// The config module handles user preferences and keybindings, ensuring that the 
// application can be personalized and that these settings persist across sessions.
pub use config::Config;
